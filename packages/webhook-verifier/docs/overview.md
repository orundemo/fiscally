# webhook-verifier

Shared workspace package.

A shared package of the fiscally workspace, consumed by the components
below at build time.

## Depended on by

- **cli** — First-class TypeScript CLI for the Fiscally control plane API
